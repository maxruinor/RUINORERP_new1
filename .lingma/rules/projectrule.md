你现在需要基于现有 ERP 项目代码进行重构与迭代开发，严格遵守以下所有约束规则，全程执行：
代码理解优先：先完整读懂当前已有业务代码、数据库结构、原有逻辑流程、BOM / 进销存 / 制令单 / 库存 / 事务等原有业务规则，禁止脱离现有项目上下文凭空编写。
极简重构原则：拒绝过度设计、多层嵌套冗余架构、无用抽象、过度解耦、过度封装、重复方法 / 重复变量 / 重复逻辑，不引入多余中间层、无用实体、冗余校验。
增量优化改造：只在当前原有代码基础上做局部优化、修复缺陷、性能提升、逻辑规范化，不全盘推翻重写、不擅自修改原有稳定业务流程，保留原有成熟业务逻辑。
代码简洁高效：避免冗余判断、重复查询、无效循环、多余注释、冗余字段、重复事务定义；合理复用现有公共方法、工具类、仓储逻辑，不重复造轮子。
业务贴合优先：ERP 为生产进销存企业级系统，优先保证业务正确性、数据一致性、事务完整性，优化以「修复 bug、提升性能、规范写法、降低耦合、优化 SQL、解决死锁 / 截断 / 并发问题」为核心。
写法规范克制：按需设计，只用必要的接口、泛型、异步改造、仓储模式；非必要不新增拓展类、不滥用设计模式、不堆砌高级语法，保证代码易维护、易阅读、适配原有项目技术栈。
符合 windows编程的规范。如设计器类的业务类分离，控件的声明定义，布局。
不能使用脚本进行批量替换操作，会导致中文乱码。