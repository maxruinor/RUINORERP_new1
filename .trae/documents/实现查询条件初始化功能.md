## 实现查询条件初始化功能

### 1. 添加初始化按钮到窗体
- 在 `frmQueryConditionSetting.Designer.cs` 中添加一个新的 `KryptonButton` 控件
- 将按钮放置在"确定"和"取消"按钮附近，保持UI布局合理
- 设置按钮属性：名称 `btnInitialize`，文本 "初始化查询条件"

### 2. 实现按钮点击事件
- 在 `frmQueryConditionSetting.cs` 中添加 `btnInitialize_Click` 事件处理方法
- 实现初始化逻辑：
  - 重置所有查询条件的 `IsVisble` 为 `true`（默认显示所有条件）
  - 重置所有查询条件的排序 `Sort` 为默认值
  - 清空所有查询条件的默认值
  - 更新 ListView 显示状态
  - 刷新编辑面板

### 3. 完善初始化逻辑
- 确保初始化时保持硬编码的过滤条件为标准
- 与 `UIBizService.cs` 中的条件生成逻辑保持一致
- 处理 `tb_UIQueryCondition` 对象的所有相关属性

### 4. 添加确认提示
- 在初始化前添加确认提示，防止误操作
- 使用专业的 MessageBox 提示语

### 5. 代码优化和注释
- 确保代码符合项目规范和格式要求
- 添加必要的注释说明初始化功能
- 保持代码简洁和可维护性

### 6. 验证功能完整性
- 确保初始化功能在不同查询实体和条件组合下正常工作
- 验证与现有代码的兼容性
- 测试初始化后的数据保存和恢复功能