### 1. 服务器端优化
**删除不必要的配置变更事件**
- **文件**：`RUINORERP.Server\frmMainNew.cs`
- **修改内容**：删除第629-632行的`Globalconfig.OnChange`事件订阅，因为该事件仅输出调试信息，没有实际业务逻辑，而Startup.cs中已经实现了完整的配置变更处理

### 2. 客户端配置更新机制实现
**添加单例配置注册**
- **文件**：`RUINORERP.UI\Startup.cs`
- **修改内容**：在`ConfigureAppSettings`方法中添加以下配置：
  1. 为`SystemGlobalConfig`和`GlobalValidatorConfig`注册单例实例
  2. 订阅`IOptionsMonitor.OnChange`事件，实时更新单例实例
  3. 确保配置路径中的环境变量正确解析

**更新应用上下文配置**
- **文件**：`RUINORERP.UI\MainForm.cs`
- **修改内容**：确保配置变更能正确更新`AppContext.SystemGlobalConfig`

### 3. 实现细节
**服务器端修改**：
- 删除frmMainNew.cs中仅用于调试的配置变更事件
- 保留Startup.cs中完整的配置变更处理逻辑

**客户端修改**：
```csharp
// 在Startup.cs的ConfigureAppSettings方法中添加

// 注册SystemGlobalConfig单例实例，支持配置变更通知
services.AddSingleton<SystemGlobalConfig>(provider =>
{
    var monitor = provider.GetRequiredService<IOptionsMonitor<SystemGlobalConfig>>();
    var systemConfig = monitor.CurrentValue;
    
    // 订阅配置变更事件，实时更新单例实例
    monitor.OnChange(newConfig =>
    {
        systemConfig = newConfig;
    });
    
    return systemConfig;
});

// 注册GlobalValidatorConfig单例实例，支持配置变更通知
services.AddSingleton<GlobalValidatorConfig>(provider =>
{
    var monitor = provider.GetRequiredService<IOptionsMonitor<GlobalValidatorConfig>>();
    var validatorConfig = monitor.CurrentValue;
    
    // 订阅配置变更事件，实时更新单例实例
    monitor.OnChange(newConfig =>
    {
        validatorConfig = newConfig;
    });
    
    return validatorConfig;
});
```

### 4. 预期效果
- 服务器端删除不必要的代码，保持配置更新逻辑的统一性
- 客户端实现与服务器端一致的配置更新机制
- 确保单例配置实例始终获取到最新配置值
- 支持配置文件热重载，无需重启应用即可生效