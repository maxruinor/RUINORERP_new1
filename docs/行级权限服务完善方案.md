# 行级权限服务完善方案

## 📋 目录
- [一、当前实现状态评估](#一当前实现状态评估)
- [二、存在的主要问题](#二存在的主要问题)
- [三、完善方案(分阶段实施)](#三完善方案分阶段实施)
- [四、测试验证方案](#四测试验证方案)
- [五、部署与监控](#五部署与监控)

---

## 一、当前实现状态评估

### ✅ 已完成的核心功能

#### 1. 架构设计完整
- **接口体系**: `IRowAuthService` 定义了清晰的权限服务契约
- **核心服务**: `RowAuthService` 实现了权限过滤条件生成逻辑
- **规则提供者**: `IDefaultRowAuthRuleProvider` 支持默认规则配置
- **参数化支持**: `ParameterizedFilterHelper` 实现动态参数替换
- **上下文管理**: `RowAuthContext` 封装权限解析所需信息

#### 2. 数据模型完备
```csharp
// tb_RowAuthPolicy - 权限策略表
- PolicyId: 策略ID (主键)
- PolicyName: 规则名称
- TargetTable/TargetEntity: 目标表/实体
- IsJoinRequired: 是否需要联表
- FilterClause: 静态过滤条件
- ParameterizedFilterClause: 参数化过滤条件模板
- IsParameterized: 是否使用参数化
- DefaultRuleEnum: 默认规则枚举

// tb_P4RowAuthPolicyByRole - 角色关联表
- Policy_Role_RID: 关联ID (主键)
- PolicyId: 策略ID (外键)
- RoleID: 角色ID (外键)
- MenuID: 菜单ID (外键) ⚠️ 关键字段
- IsEnabled: 是否启用
```

#### 3. 业务规则管理
- **BizTypeRuleManager**: 已配置15+业务类型的规则映射
  - 销售类: 仅客户数据、全部数据
  - 采购类: 仅供应商数据、全部数据
  - 财务类: 收款/付款/应收/应付等多种规则
  - 生产类: 仅负责人数据、全部数据
  
- **7种预定义规则**:
  ```csharp
  OnlyCustomer = 1      // 仅客户数据
  OnlySupplier = 2      // 仅供应商数据
  AllDataForOtherInOut = 3  // 其他出入库全部数据
  OnlyReceivable = 4    // 仅收款数据
  OnlyPayable = 5       // 仅付款数据
  OnlyOwner = 6         // 仅自己负责的数据
  AllData = 7           // 全部数据
  ```

#### 4. 缓存机制
- MemoryCache实现,带滑动过期(30分钟)和绝对过期(2小时)
- 缓存键格式: `RowAuth:UserId:{userId}:Entity:{entityName}:Role:{roleId}:Menu:{menuId}`
- 配置变更时自动清除相关缓存(`ClearAuthCache`)

#### 5. 实际应用集成
- `BaseBillQueryMC.cs` 第2133行已调用获取过滤条件
- 支持在查询时应用行级权限过滤

---

## 二、存在的主要问题

### 🔴 P0级问题(必须修复)

#### 1. ❌ 缺少实际过滤应用逻辑
**问题描述**: 
- `BaseBillQueryMC.cs` 中只获取了`filterClause`,但没有真正应用到SqlSugar查询中
- 原有的复杂处理逻辑(ContainsComplexSqlStructure等)已被简化但未正确实现

**影响**: 行级权限完全不起作用,用户可以查看所有数据

**修复方案**:
```csharp
// 在 BaseBillQueryMC.cs 中修改
var sqlSugarFilter = new SqlSugarRowLevelAuthFilter(MainForm.Instance.logger);
string whereClause = sqlSugarFilter.BuildFilterClause<M>(filterClause);

if (!string.IsNullOrEmpty(whereClause))
{
    LimitQueryConditions = MergeQueryConditions(LimitQueryConditions, whereClause);
}
```

✅ **已修复**: 已在`BaseBillQueryMC.cs`中应用正确的过滤逻辑

---

#### 2. ❌ Expression转换无效
**问题描述**:
- `ExpressionFilterHelper.ConvertToExpression<T>()` 始终返回 `t => true`
- 无法将SQL字符串转换为Lambda表达式
- 导致`GetUserRowAuthFilterExpression`方法失效

**影响**: 无法使用Expression形式的权限过滤

**修复方案**:
```csharp
// 方案1: 废弃Expression方式,统一使用SQL字符串
// 方案2: 实现真正的SQL到Expression转换器(复杂,不推荐)
// 建议: 采用方案1,因为SqlSugar的Where方法直接支持SQL字符串
```

**状态**: 已通过使用`SqlSugarRowLevelAuthFilter.BuildFilterClause`解决

---

#### 3. ❌ SqlSugarRowLevelAuthFilter未被使用
**问题描述**:
- `SqlSugarRowLevelAuthFilter` 类已实现完整的过滤逻辑
- 包含EXISTS子查询、IN子查询、简单条件的处理
- 但之前的代码未调用此类

**影响**: 复杂的权限规则(如需要关联表的规则)无法正确应用

**修复方案**:
✅ **已修复**: 在`BaseBillQueryMC.cs`中已正确调用`BuildFilterClause<M>()`

---

#### 4. ⚠️ 菜单级别权限未充分验证
**问题描述**:
- `tb_P4RowAuthPolicyByRole`包含`MenuID`字段
- `GetPoliciesByUserAndMenu`中使用了该字段进行过滤
- 需要确认前端是否正确传递MenuID

**验证要点**:
- 检查`CurMenuInfo.MenuID`是否正确获取
- 确认数据库中是否有针对特定菜单的权限配置
- 测试不同菜单下同一实体的权限差异

---

### 🟡 P1级问题(重要优化)

#### 5. ❌ 参数化规则解析器缺失
**问题描述**:
- `DefaultRowAuthRuleProvider`中OnlyOwner规则使用了`{EmployeeId}`占位符
- 但`RuleParameterResolver`类不存在(之前未创建)

**影响**: 参数化规则无法正常工作

**修复方案**:
✅ **已修复**: 已创建`RuleParameterResolver.cs`,支持以下参数:
- `{UserId}`: 用户ID
- `{RoleId}`: 角色ID
- `{EmployeeId}`: 员工ID
- `{DepartmentId}`: 部门ID
- `{MenuId}`: 菜单ID
- `{RoleIds}`: 角色ID列表(用于IN查询)
- 支持通过`ExtendedProperties`扩展自定义参数

---

#### 6. ⚠️ SQL注入防护不够完善
**问题描述**:
- 原`ContainsPotentialSqlInjection`仅检查简单关键字
- 无法防止复杂的注入攻击

**修复方案**:
✅ **已增强**: 
- 增加更多危险关键字(xp_, sp_, 0x, --, ;, /*, */)
- 使用单词边界匹配,避免误判
- 检查连续特殊字符序列
- 记录详细的警告日志

---

#### 7. ❌ 缺少用户级权限支持
**问题描述**:
- `GetPoliciesByUserAndMenu`中用户级权限被注释掉
- 缺少`tb_P4RowAuthPolicyByUser`表的实际应用

**影响**: 只能基于角色分配权限,无法为特定用户设置例外

**修复方案**:
```csharp
// 在 GetPoliciesByUserAndMenu 中取消注释并实现
var userPolicies = _db.Queryable<tb_RowAuthPolicy>()
    .InnerJoin<tb_P4RowAuthPolicyByUser>((p, u) => p.PolicyId == u.PolicyId)
    .Where((p, u) => p.IsEnabled && u.User_ID == userId && u.MenuID == menuId)
    .Select((p, u) => p)
    .ToList();
policies.AddRange(userPolicies);
```

**优先级**: 中(当前角色级权限已满足大部分需求)

---

#### 8. ⚠️ 错误处理不够健壮
**问题描述**:
- 多处catch后返回空值或默认值,可能导致静默失败
- 缺少详细的错误日志和告警机制

**修复方案**:
- 增加更详细的异常日志记录
- 关键操作失败时发送告警通知
- 提供降级策略(如权限服务失败时返回全部数据并记录警告)

---

### 🟢 P2级问题(体验优化)

#### 9. 缺少可视化配置界面
**现状**: 虽然有`UCRowAuthPolicyEditEnhanced`,但未见完整的管理界面

**建议**:
- 创建专门的"行级权限管理"菜单
- 提供规则配置向导
- 支持规则预览和测试
- 显示权限生效范围

---

#### 10. 性能监控缺失
**建议**:
- 记录权限过滤的执行时间
- 统计缓存命中率
- 识别慢查询和优化点
- 提供性能报表

---

## 三、完善方案(分阶段实施)

### 阶段一:核心功能补全(1-2天) ✅ 已完成

#### 任务清单:
- [x] 修复查询过滤应用逻辑
- [x] 创建RuleParameterResolver参数解析器
- [x] 增强SQL注入防护
- [x] 集成SqlSugarRowLevelAuthFilter
- [x] 更新BaseBillQueryMC中的实际应用

#### 交付物:
1. ✅ `RuleParameterResolver.cs` - 参数解析器
2. ✅ 增强的`RowAuthService.cs` - SQL注入防护
3. ✅ 更新的`BaseBillQueryMC.cs` - 正确的过滤应用

---

### 阶段二:功能增强(2-3天)

#### 1. 实现用户级权限支持

**步骤**:
1. 确保数据库中存在`tb_P4RowAuthPolicyByUser`表
2. 在`RowAuthService.GetPoliciesByUserAndMenu`中启用用户级权限查询
3. 在前端添加用户级权限配置界面
4. 测试用户级权限与角色级权限的优先级(建议:用户级优先)

**代码示例**:
```csharp
private List<tb_RowAuthPolicy> GetPoliciesByUserAndMenu(long userId, List<long> roleIds, long menuId)
{
    var policies = new List<tb_RowAuthPolicy>();

    // 1. 先获取用户级权限(优先级更高)
    var userPolicies = _db.Queryable<tb_RowAuthPolicy>()
        .InnerJoin<tb_P4RowAuthPolicyByUser>((p, u) => p.PolicyId == u.PolicyId)
        .Where((p, u) => p.IsEnabled && u.User_ID == userId && u.MenuID == menuId)
        .Select((p, u) => p)
        .ToList();
    policies.AddRange(userPolicies);

    // 2. 再获取角色级权限
    if (roleIds != null && roleIds.Any())
    {
        var rolePolicies = _db.Queryable<tb_RowAuthPolicy>()
            .InnerJoin<tb_P4RowAuthPolicyByRole>((p, r) => p.PolicyId == r.PolicyId)
            .Where((p, r) => p.IsEnabled && roleIds.Contains(r.RoleID) && r.MenuID == menuId)
            .Select((p, r) => p)
            .ToList();
        policies.AddRange(rolePolicies);
    }

    return policies;
}
```

---

#### 2. 优化缓存策略

**当前问题**:
- 缓存键过于细化,可能导致缓存碎片
- 缺少缓存预热机制

**优化方案**:
```csharp
// 1. 简化缓存键(如果不需要菜单级别区分)
private string GenerateCacheKey(string entityName, long roleId)
{
    return $"RowAuth:Entity:{entityName}:Role:{roleId}";
}

// 2. 添加缓存预热(在用户登录时)
public void WarmupAuthCache(long userId)
{
    var userRoles = GetUserRoles(userId);
    foreach (var role in userRoles)
    {
        // 预加载常用实体的权限配置
        var commonEntities = new[] { "SalesOrder", "PurchaseOrder", "Invoice" };
        foreach (var entity in commonEntities)
        {
            GetUserRowAuthFilterClause(Type.GetType(entity), 0);
        }
    }
}

// 3. 增加缓存统计
private static int _cacheHitCount = 0;
private static int _cacheMissCount = 0;

public double GetCacheHitRate()
{
    int total = _cacheHitCount + _cacheMissCount;
    return total > 0 ? (double)_cacheHitCount / total : 0;
}
```

---

#### 3. 添加性能监控

**实现方案**:
```csharp
public class RowAuthPerformanceMonitor
{
    private static readonly ConcurrentDictionary<string, List<long>> _executionTimes = 
        new ConcurrentDictionary<string, List<long>>();

    public static void RecordExecutionTime(string operation, long elapsedMs)
    {
        _executionTimes.AddOrUpdate(
            operation,
            new List<long> { elapsedMs },
            (key, list) => { list.Add(elapsedMs); return list; });
    }

    public static PerformanceStats GetStats(string operation)
    {
        if (!_executionTimes.TryGetValue(operation, out var times) || !times.Any())
            return new PerformanceStats();

        return new PerformanceStats
        {
            Operation = operation,
            Count = times.Count,
            AvgMs = times.Average(),
            MaxMs = times.Max(),
            MinMs = times.Min(),
            P95Ms = CalculatePercentile(times, 95)
        };
    }
}

// 在 RowAuthService 中使用
public string GetUserRowAuthFilterClause(Type entityType, long menuId)
{
    var stopwatch = Stopwatch.StartNew();
    try
    {
        // ... 原有逻辑 ...
    }
    finally
    {
        stopwatch.Stop();
        RowAuthPerformanceMonitor.RecordExecutionTime(
            "GetUserRowAuthFilterClause", 
            stopwatch.ElapsedMilliseconds);
    }
}
```

---

### 阶段三:用户体验优化(3-5天)

#### 1. 创建可视化管理界面

**功能模块**:
- 权限规则列表查看
- 规则配置向导
- 规则预览和测试
- 批量分配权限
- 权限继承关系图

**界面布局建议**:
```
┌─────────────────────────────────────────────┐
│  行级权限管理                                │
├──────────┬──────────────────────────────────┤
│          │                                  │
│ 业务类型  │  ┌─ 可用规则 ────────────────┐  │
│ 列表     │  │ ☑ 仅客户数据               │  │
│          │  │ ☐ 仅供应商数据             │  │
│ • 销售订单│  │ ☐ 仅负责人数据             │  │
│ • 采购订单│  │ ☐ 全部数据                 │  │
│ • 收款单  │  └──────────────────────────┘  │
│          │                                  │
│          │  ┌─ 已分配角色 ──────────────┐  │
│          │  │ • 销售经理 (已配置)        │  │
│          │  │ • 采购经理 (未配置)        │  │
│          │  └──────────────────────────┘  │
│          │                                  │
│          │  [保存配置] [测试规则] [取消]   │
└──────────┴──────────────────────────────────┘
```

---

#### 2. 添加规则测试功能

**实现思路**:
```csharp
public class RowAuthTester
{
    /// <summary>
    /// 测试权限规则对特定用户的过滤效果
    /// </summary>
    public TestResult TestRule(long userId, long menuId, Type entityType)
    {
        var result = new TestResult();
        
        // 1. 获取用户会应用的权限规则
        var filterClause = _rowAuthService.GetUserRowAuthFilterClause(entityType, menuId);
        result.FilterClause = filterClause;
        
        // 2. 执行测试查询(限制返回数量)
        var testQuery = _db.Queryable(entityType)
            .Where(filterClause ?? "1=1")
            .Take(10);
        
        result.SampleData = testQuery.ToList();
        result.TotalCount = _db.Queryable(entityType)
            .Where(filterClause ?? "1=1")
            .Count();
        
        // 3. 对比无权限限制的总数
        result.UnrestrictedCount = _db.Queryable(entityType).Count();
        
        return result;
    }
}

public class TestResult
{
    public string FilterClause { get; set; }
    public List<object> SampleData { get; set; }
    public int TotalCount { get; set; }
    public int UnrestrictedCount { get; set; }
    public double FilterRatio => UnrestrictedCount > 0 
        ? (double)TotalCount / UnrestrictedCount 
        : 0;
}
```

---

#### 3. 提供权限诊断工具

**功能**:
- 查看某个用户对某实体的完整权限链
- 分析权限规则的生效情况
- 检测权限冲突
- 生成权限审计报告

**示例输出**:
```
用户: 张三 (UserID: 1001)
实体: 销售订单 (SalesOrder)
菜单: 销售订单查询 (MenuID: 5001)

权限来源:
  1. 角色: 销售经理 (RoleID: 101)
     - 规则: 仅客户数据
     - 过滤条件: EXISTS (SELECT 1 FROM tb_CustomerVendor cv 
                        WHERE cv.CustomerVendor_ID = SalesOrder.CustomerVendor_ID 
                        AND cv.IsCustomer = 1)
  
  2. 用户级例外: 无

最终过滤条件:
  EXISTS (SELECT 1 FROM tb_CustomerVendor cv 
          WHERE cv.CustomerVendor_ID = SalesOrder.CustomerVendor_ID 
          AND cv.IsCustomer = 1)

预期影响:
  - 总记录数: 10,000
  - 过滤后: 3,500
  - 过滤比例: 35%
```

---

## 四、测试验证方案

### 4.1 单元测试

#### 测试用例清单:

```csharp
[TestClass]
public class RowAuthServiceTests
{
    [TestMethod]
    public void Test_GetUserRowAuthFilterClause_SuperAdmin_ReturnsEmpty()
    {
        // 超级管理员应跳过权限过滤
        var result = _service.GetUserRowAuthFilterClause(typeof(SalesOrder), 1001);
        Assert.IsNull(result);
    }

    [TestMethod]
    public void Test_GetUserRowAuthFilterClause_NoRoles_ReturnsNull()
    {
        // 无角色的用户应返回null(无限制)
        var result = _service.GetUserRowAuthFilterClause(typeof(SalesOrder), 1001);
        Assert.IsNull(result);
    }

    [TestMethod]
    public void Test_GetUserRowAuthFilterClause_OnlyCustomerRule_ReturnsExistsClause()
    {
        // 配置"仅客户数据"规则
        var result = _service.GetUserRowAuthFilterClause(typeof(SalesOrder), 1001);
        Assert.IsNotNull(result);
        Assert.IsTrue(result.Contains("EXISTS"));
        Assert.IsTrue(result.Contains("IsCustomer = 1"));
    }

    [TestMethod]
    public void Test_ParameterizedFilter_EmployeeId_ReplacesCorrectly()
    {
        // 测试参数化规则
        var context = new RowAuthContext { EmployeeId = 123 };
        var template = "Employee_ID = {EmployeeId}";
        var resolved = ParameterizedFilterHelper.ResolveFilterTemplate(template, context);
        Assert.AreEqual("Employee_ID = 123", resolved);
    }

    [TestMethod]
    public void Test_SQLInjection_PreventsDangerousKeywords()
    {
        // 测试SQL注入防护
        var maliciousClause = "1=1; DROP TABLE Users;--";
        var result = _service.GetUserRowAuthFilterClause(typeof(SalesOrder), 1001);
        // 应该被拒绝或清理
        Assert.IsFalse(result.Contains("DROP"));
    }

    [TestMethod]
    public void Test_Cache_HitsOnSecondCall()
    {
        // 第一次调用
        var result1 = _service.GetUserRowAuthFilterClause(typeof(SalesOrder), 1001);
        // 第二次调用应从缓存获取
        var result2 = _service.GetUserRowAuthFilterClause(typeof(SalesOrder), 1001);
        Assert.AreEqual(result1, result2);
    }
}
```

---

### 4.2 集成测试

#### 测试场景:

**场景1: 销售经理查看销售订单**
```
前置条件:
- 用户A属于"销售经理"角色
- "销售经理"角色对"销售订单"配置了"仅客户数据"规则

测试步骤:
1. 以用户A登录
2. 打开"销售订单查询"菜单
3. 执行查询

预期结果:
- 只显示与客户相关的订单
- 不显示与供应商相关的订单
- SQL中包含EXISTS子查询
```

**场景2: 采购经理查看采购订单**
```
前置条件:
- 用户B属于"采购经理"角色
- "采购经理"角色对"采购订单"配置了"仅供应商数据"规则

测试步骤:
1. 以用户B登录
2. 打开"采购订单查询"菜单
3. 执行查询

预期结果:
- 只显示与供应商相关的订单
- SQL中包含EXISTS (SELECT 1 FROM tb_CustomerVendor WHERE IsVendor = 1)
```

**场景3: 财务人员查看收款单**
```
前置条件:
- 用户C属于"财务专员"角色
- "财务专员"角色对"收款单"配置了"仅收款数据"规则

测试步骤:
1. 以用户C登录
2. 打开"收款单查询"菜单
3. 执行查询

预期结果:
- 只显示ReceivePaymentType = 1的记录
- 不显示付款类型的记录
```

**场景4: 多角色用户**
```
前置条件:
- 用户D同时属于"销售经理"和"财务专员"角色
- 两个角色对"销售订单"有不同的权限规则

测试步骤:
1. 以用户D登录
2. 打开"销售订单查询"菜单
3. 执行查询

预期结果:
- 应用多个规则的OR组合
- 可以看到任一角色允许的数据
```

**场景5: 菜单级别权限差异**
```
前置条件:
- 同一用户对同一实体在不同菜单有不同权限
- 菜单A: 查看全部数据
- 菜单B: 仅看客户数据

测试步骤:
1. 在菜单A中查询
2. 在菜单B中查询

预期结果:
- 菜单A返回所有数据
- 菜单B只返回客户数据
- 两次查询的过滤条件不同
```

---

### 4.3 性能测试

#### 测试指标:

```csharp
[TestMethod]
public void PerformanceTest_AuthFilter_ShouldBeFast()
{
    var stopwatch = Stopwatch.StartNew();
    
    for (int i = 0; i < 1000; i++)
    {
        _service.GetUserRowAuthFilterClause(typeof(SalesOrder), 1001);
    }
    
    stopwatch.Stop();
    
    // 平均每次调用应小于10ms(含缓存)
    double avgMs = stopwatch.ElapsedMilliseconds / 1000.0;
    Assert.IsTrue(avgMs < 10, $"平均响应时间{avgMs}ms超过阈值10ms");
}

[TestMethod]
public void PerformanceTest_FirstCall_ShouldBeAcceptable()
{
    // 首次调用(无缓存)应在100ms内完成
    var stopwatch = Stopwatch.StartNew();
    _service.GetUserRowAuthFilterClause(typeof(SalesOrder), 1001);
    stopwatch.Stop();
    
    Assert.IsTrue(stopwatch.ElapsedMilliseconds < 100, 
        $"首次调用耗时{stopwatch.ElapsedMilliseconds}ms超过阈值100ms");
}
```

---

### 4.4 安全测试

#### 渗透测试要点:

1. **SQL注入测试**
   ```
   尝试在自定义规则中输入:
   - 1=1; DROP TABLE tb_SalesOrder;--
   - 1=1 UNION SELECT * FROM tb_UserInfo
   - ' OR '1'='1
   
   预期: 系统应拒绝或清理这些输入
   ```

2. **越权访问测试**
   ```
   尝试:
   - 普通用户尝试访问管理员专属数据
   - 销售人员尝试查看其他销售的私有数据
   
   预期: 权限过滤应正确阻止访问
   ```

3. **缓存污染测试**
   ```
   尝试:
   - 修改数据库中的权限配置
   - 立即查询验证缓存是否及时失效
   
   预期: 缓存应在配置变更后清除
   ```

---

## 五、部署与监控

### 5.1 部署检查清单

#### 数据库迁移:
```sql
-- 1. 确认表结构完整
SELECT COUNT(*) FROM tb_RowAuthPolicy;
SELECT COUNT(*) FROM tb_P4RowAuthPolicyByRole;
SELECT COUNT(*) FROM tb_P4RowAuthPolicyByUser; -- 如果启用了用户级权限

-- 2. 初始化默认规则
INSERT INTO tb_RowAuthPolicy (PolicyName, TargetTable, TargetEntity, IsJoinRequired, 
    JoinTable, JoinOnClause, FilterClause, IsEnabled, DefaultRuleEnum)
VALUES 
('销售订单-仅客户数据', 'tb_SalesOrder', 'SalesOrder', 1, 
 'tb_CustomerVendor', 'tb_SalesOrder.CustomerVendor_ID = tb_CustomerVendor.CustomerVendor_ID',
 'tb_CustomerVendor.IsCustomer = 1', 1, 1),
('采购订单-仅供应商数据', 'tb_PurchaseOrder', 'PurchaseOrder', 1,
 'tb_CustomerVendor', 'tb_PurchaseOrder.CustomerVendor_ID = tb_CustomerVendor.CustomerVendor_ID',
 'tb_CustomerVendor.IsVendor = 1', 1, 2);

-- 3. 为角色分配规则
INSERT INTO tb_P4RowAuthPolicyByRole (PolicyId, RoleID, MenuID, IsEnabled)
SELECT p.PolicyId, r.RoleID, m.MenuID, 1
FROM tb_RowAuthPolicy p
CROSS JOIN tb_RoleInfo r
CROSS JOIN tb_MenuInfo m
WHERE r.RoleName = '销售经理' 
  AND m.MenuName = '销售订单查询'
  AND p.DefaultRuleEnum = 1;
```

---

#### 配置文件更新:
```xml
<!-- app.config 或 appsettings.json -->
{
  "RowLevelAuth": {
    "Enabled": true,
    "CacheSlidingExpirationMinutes": 30,
    "CacheAbsoluteExpirationHours": 2,
    "EnableUserLevelAuth": false,  // 是否启用用户级权限
    "EnablePerformanceMonitoring": true,
    "SqlInjectionProtectionLevel": "Strict"
  }
}
```

---

#### 代码部署:
1. ✅ 编译`RUINORERP.Business`项目
2. ✅ 编译`RUINORERP.UI`项目
3. ✅ 部署DLL到客户端目录
4. ✅ 重启客户端应用
5. ✅ 清除旧缓存(如有必要)

---

### 5.2 监控指标

#### 关键监控项:

```csharp
public class RowAuthMetrics
{
    // 1. 权限过滤调用次数
    public static long TotalFilterCalls { get; private set; }
    
    // 2. 缓存命中率
    public static double CacheHitRate { get; private set; }
    
    // 3. 平均响应时间
    public static double AvgResponseTimeMs { get; private set; }
    
    // 4. 权限拒绝次数(过滤掉的数据比例)
    public static double AvgFilterRatio { get; private set; }
    
    // 5. SQL注入拦截次数
    public static long SqlInjectionBlockedCount { get; private set; }
    
    // 6. 错误次数
    public static long ErrorCount { get; private set; }
}
```

---

#### 日志记录:

```csharp
// 关键操作日志
_logger.LogInformation("行级权限过滤应用: User={UserId}, Entity={Entity}, Menu={MenuId}, FilterClause={Filter}",
    userId, entityType.Name, menuId, filterClause);

// 性能日志
_logger.LogDebug("权限过滤耗时: {ElapsedMs}ms, CacheHit={CacheHit}",
    elapsedMs, cacheHit);

// 错误日志
_logger.LogError(ex, "权限过滤失败: User={UserId}, Entity={Entity}, Error={Error}",
    userId, entityType.Name, ex.Message);

// 安全日志
_logger.LogWarning("检测到潜在SQL注入: User={UserId}, Clause={Clause}",
    userId, maliciousClause);
```

---

### 5.3 告警规则

#### 建议设置的告警:

| 指标 | 阈值 | 告警级别 | 处理方式 |
|------|------|----------|----------|
| 平均响应时间 > 100ms | 持续5分钟 | Warning | 检查数据库索引 |
| 错误率 > 5% | 持续10分钟 | Critical | 立即排查 |
| SQL注入拦截 > 10次/小时 | 单次触发 | Critical | 安全团队介入 |
| 缓存命中率 < 50% | 持续30分钟 | Warning | 调整缓存策略 |
| 权限服务不可用 | 即时 | Critical | 降级为全部数据 |

---

### 5.4 故障应急预案

#### 场景1: 权限服务完全失效

**症状**: 所有查询都失败或超时

**应急措施**:
```csharp
// 在 RowAuthService 中添加降级逻辑
public string GetUserRowAuthFilterClause(Type entityType, long menuId)
{
    try
    {
        // 正常逻辑...
    }
    catch (Exception ex)
    {
        _logger.LogCritical(ex, "行级权限服务严重故障,启用降级模式");
        
        // 降级策略1: 返回null(无限制)
        return null;
        
        // 降级策略2: 返回"1=1"(全部数据)
        // return "1=1";
        
        // 降级策略3: 返回最严格的限制
        // return "1=0"; // 不显示任何数据
    }
}
```

---

#### 场景2: 缓存污染导致权限错误

**症状**: 用户看到不应看到的数据

**应急措施**:
```bash
# 1. 清除所有权限缓存
# 在管理界面或通过API调用
POST /api/rowauth/clear-all-cache

# 2. 重启应用(如果必要)
# 3. 检查数据库中的权限配置是否正确
```

---

#### 场景3: 性能急剧下降

**症状**: 查询响应时间从100ms增加到5秒

**排查步骤**:
1. 检查数据库慢查询日志
2. 验证EXISTS子查询是否有合适的索引
3. 检查缓存是否失效导致频繁查询数据库
4. 临时禁用行级权限(紧急情况下)

**优化措施**:
```sql
-- 为常用关联字段添加索引
CREATE INDEX IX_CustomerVendor_IsCustomer ON tb_CustomerVendor(IsCustomer);
CREATE INDEX IX_CustomerVendor_IsVendor ON tb_CustomerVendor(IsVendor);
CREATE INDEX IX_SalesOrder_CustomerVendor_ID ON tb_SalesOrder(CustomerVendor_ID);
```

---

## 六、总结与建议

### 6.1 当前进度

✅ **已完成**:
- 核心架构设计和实现
- 数据模型建立
- 业务规则管理
- 缓存机制
- 参数化规则支持
- SQL注入防护增强
- 查询过滤应用修复

⏳ **待完成**:
- 用户级权限支持
- 可视化管理界面
- 性能监控系统
- 完整的测试套件
- 文档完善

---

### 6.2 下一步行动

**短期(1周内)**:
1. 部署当前修复到测试环境
2. 执行集成测试
3. 收集性能数据
4. 修复发现的问题

**中期(1个月内)**:
1. 实现用户级权限
2. 开发管理界面
3. 添加性能监控
4. 编写用户手册

**长期(3个月内)**:
1. 优化性能和缓存策略
2. 扩展更多预定义规则
3. 实现权限审计功能
4. 支持更复杂的权限场景(如部门级、项目组级)

---

### 6.3 最佳实践建议

1. **权限设计原则**
   - 最小权限原则: 默认拒绝,显式授权
   - 职责分离: 不同角色有不同权限
   - 定期审查: 每季度审核权限配置

2. **开发规范**
   - 所有数据查询必须经过权限过滤
   - 禁止绕过权限服务的直接SQL查询
   - 新增业务类型时必须配置权限规则

3. **运维建议**
   - 监控权限服务的健康状态
   - 定期清理无用权限配置
   - 备份权限配置数据

4. **安全建议**
   - 定期更新SQL注入防护规则
   - 审计权限变更日志
   - 对敏感数据进行额外加密

---

## 附录

### A. 相关文档链接
- [SqlSugar官方文档](http://www.donet5.com/)
- [行级权限设计原理](./docs/行级权限设计说明.md)
- [API参考手册](./docs/RowAuthService-API.md)

### B. 常见问题FAQ

**Q1: 为什么我的权限规则没有生效?**
A: 检查以下几点:
- 用户是否有分配角色
- 角色是否配置了权限规则
- 缓存是否已清除
- 数据库中规则是否启用(IsEnabled=1)

**Q2: 如何调试权限过滤?**
A: 
- 查看日志中的FilterClause输出
- 使用RuleTester工具测试
- 直接在数据库中执行生成的SQL

**Q3: 性能受影响怎么办?**
A:
- 确保关联字段有索引
- 调整缓存过期时间
- 考虑简化复杂规则
- 启用性能监控定位瓶颈

**Q4: 如何为特定用户设置例外?**
A: 启用用户级权限功能,为该用户配置特殊的权限规则(优先级高于角色权限)

---

### C. 版本历史

| 版本 | 日期 | 变更内容 | 作者 |
|------|------|----------|------|
| 1.0 | 2026-04-29 | 初始版本,完成核心功能修复 | Lingma |

---

**文档结束**
